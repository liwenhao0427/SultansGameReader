# 苏丹的游戏本地剧情阅读器 - 协作约束

## 项目目标

本项目的目标不是完整复刻游戏系统，而是做一个**快捷、方便、美观**的本地剧情阅读器。

阅读器重点服务以下内容：

- 仪式
- 事件
- 后续分支
- 相关图片资源

卡牌、物品、数值系统本身不是核心玩法，不需要完整模拟；它们主要用于：

- 解释事件或仪式中的条件
- 展示相关图片
- 说明某个分支为什么出现

---

## 当前实现方向

### 1. 主展示模式

阅读器采用两种互补的展示方式：

- 节点图：用于查看事件、仪式、后续触发关系
- 类视觉小说界面：用于阅读当前节点正文、分支、图片和后续推进

### 2. 仪式展示原则

- 仪式是最重要的阅读对象
- 仪式进入后，优先展示 `text`
- 左侧列出该仪式的卡槽
- 用户点击卡槽后，展示该槽位可能触发的分支、台词或结算
- 若某分支涉及属性检定，不需要真实计算，只需要允许用户手动选择“成功”或“失败”

### 3. 事件展示原则

- 事件按正文阅读体验展示
- 有正文文本的事件显示为可读节点
- 没有实际正文、只有触发功能的事件，不作为独立正文节点，可仅作为流程连接
- 事件分支要能继续引出后续事件或仪式

### 4. 图片资源展示原则

- 尽可能加载并展示对应图片资源
- 仪式、事件、后日谈中只要存在合适图片，就优先配图展示
- 卡牌和物品即使不是阅读核心，也可以在相关分支中展示图片或简要说明

### 5. UI 风格原则

- 尽量贴近游戏原本的阅读气质
- 可优先复用 `resource/Sprite/` 中已有背景板与装饰图
- 仪式文本区、卡槽区、说明区应明显分区
- 不追求复杂交互，优先保证直观、稳定、易读

---

## 数据使用约束

- 前端只依赖 `cache/` 和 `resource/`
- 不在渲染层直接解析原始 `config/`
- 条件表达式保留原始 key，在展示层转成人类可读文本
- 图片查找必须兼容 `.png` 和 `.png.png`
- 前端涉及 `resource/` 的常量资源名（如卡牌稀有度底板、阅读器固定背景板等）统一放入配置文件维护，禁止在组件内散落硬编码
- 资源配置文件必须包含中文注释，说明资源用途、映射关系和回退规则

---

## 目录约束

以下目录只允许读取，不允许修改：

- `AssetStudio-net8.0-win/`
- `config/`
- `resource/`
- `sudans-game-reader/`

允许开发和修改的主要目录：

- `sultan-reader/`
- `相关知识/`
- `Agents.md`

---

## 开发优先级

后续开发时，优先级按下面顺序执行：

1. 仪式阅读体验
2. 事件分支阅读体验
3. 图片与背景板展示
4. 节点图联动
5. 其他辅助信息

如与完整游戏机制冲突，优先保证“易读、好用、接近原始叙事体验”。

---

## 协作约束

- 所有注释、文档、回复均使用中文
- 修改文件后，本轮对话需要提交一次 git commit
- 提交时只提交本轮实际修改且确认无误的文件，不擅自混入无关改动
- 如果发现原有设计与“剧情阅读器”目标冲突，应优先服务阅读体验，而不是保留无意义的系统复杂度
